# TEE / OP-TEE
CONFIG_TEE=y
CONFIG_OPTEE=y

# Disable the Linux RPC-IF driver so OP-TEE owns the RPC-IF controller
# exclusively (secure world driver via W77Q flash storage backend).
# CONFIG_RENESAS_RPCIF is not set
# CONFIG_SPI_RPCIF is not set
# CONFIG_RPCIF_HYPERBUS is not set
